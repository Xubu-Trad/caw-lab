# Evidence — R1-010

This folder holds **receipts** for this layer (text outputs, hashes, and small artifacts).
Do **not** commit large binaries here. Put large/raw items in **caw-lab-private** and commit only hashes + retrieval notes here.

## What belongs here
- SHA-256 of every input/output
- Tool output logs (exiftool output, zsteg output, cast receipts, etc.)
- Minimal extracted text payloads
- Links + retrieval instructions for any non-tracked inputs
