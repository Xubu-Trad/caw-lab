Text-only canon receipts for the R1 IPFS / APE stage.

This evidence folder preserves:
- `ffprobe` verification of the APE stream
- archived stripped-copy checks with sizes and hashes
- sanitized logical paths showing the preserved copy family

These receipts prove the APE stage is real and locally verified from preserved history. They do not by themselves provide the binary payload or a full public one-command rebuild.
