# Preserve the original formatting

This text comes directly from the pinned, audio-extracted Enkidu pseudohex. All 60 tabs are retained. Replacing each tab with one space exactly reproduces the public normalized manifesto; no other normalization is applied.

```sh
python3 scripts/verify_manifesto_formatting.py layers/R1-040_deepsound_enkidu/EVIDENCE/enkidu.full_pseudohex.txt --out-dir replay-formatting
```

All 2,327 alphabetic pseudohex symbols use uppercase U-Z. All 60 tabs occur in leading indentation on 33 lines. These are source observations, not proof that every possible formatting cipher is absent.

The older YALE AND ME recipe calls for an extra N from `moonning`. That word is absent. The actual `mooning` occurs at byte 7759 (zero-based), line 108. That exact historical selection fails; other typo hypotheses remain open. Full exploratory streams and competing correction choices are preserved in the private research journal.
